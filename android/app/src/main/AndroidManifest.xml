<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="photo_selector_flutter"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- F-16 Open With: file managers/gallery apps hand us a path via
                 ACTION_VIEW. content:// URI resolution to a real filesystem path
                 (and the folder-scan support needed to act on it) is NOT done
                 here; see MainActivity.kt and open_with_channel.dart doc
                 comments. This declares the OS-level entry point only.

                 Scoped to mimeType "image/*", not "*/*": review cycle 1 found
                 "*/*" advertises Halcyon as an Open With target for every file
                 type (PDF/video/zip), not just photos. "image/*" is a wildcard
                 match against the subtype, so it covers our RAW formats too;
                 Android/vendor MIME registries use "image/x-adobe-dng",
                 "image/x-canon-cr2", "image/x-sony-arw", etc., all under the
                 "image/" type. Deliberately NOT also adding pathPattern
                 extension matching: Android's intent-filter data matching
                 pools scheme/host/path/mimeType across ALL data tags in one
                 filter rather than per-tag, so a wrong pathPattern glob (its
                 minimal, non-regex "*" syntax; case-sensitive; requires an
                 explicit host to be evaluated at all for file:// URIs) is easy
                 to get subtly wrong and impossible to verify without a device
                 on this host; image/* alone already fixes the reported
                 defect without that risk. BROWSABLE dropped: this activity has
                 no web content to expose to browser-driven URL navigation, it
                 only matters for app/file-manager driven ACTION_VIEW. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="file" android:mimeType="image/*"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="content" android:mimeType="image/*"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
