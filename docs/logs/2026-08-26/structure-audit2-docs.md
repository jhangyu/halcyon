# Documentation Tree Audit #2 — docs/** and root *.md

Date: 2026-08-26
Scope: docs/** and root-level *.md (README.md, CLAUDE.md, rule.md, task.md, memory.md, handover.md, plan.md, file_index.md, unit_test.md). Read-only; no files moved/edited/deleted except this artifact.
Prior context read in full: `rule.md`, `CLAUDE.md` (Project SOP section), `docs/logs/2026-08-25/structure-audit-inventory.md`, `structure-audit-architecture.md`, `structure-audit-proposal.md`, `structure-refactor-plan.md`. Items already reported/decided there (root SOP-file placement, `docs/superpowers/` undocumented-in-rule.md convention, `tool/` vs `scripts/` split, `scripts/tmp/` gitignore) are not re-reported below except where this audit found new, more specific evidence.

## 1. Misplaced files

| Path | Finding | Evidence (file:line) | Recommendation | Severity | Confidence |
|---|---|---|---|---|---|
| `docs/flutter_app_README.md` | Stale, superseded sibling of root `README.md`. References the deleted `./scripts/build.sh` (root `README.md` and `CLAUDE.md` both use `scripts/build_apps.py`), and its "跨檔同步對象" field points at a wrong/foreign absolute path `/Users/jhangyu/Documents/Halcyon/` (actual repo root is `/Users/jhangyu/project/Halcyon/`). Dated 2026-05-01, the oldest doc in the tree, predating the `build.sh`→`build_apps.py` migration and the SOP root-doc consolidation. | `docs/flutter_app_README.md:44-47` (`./scripts/build.sh ...` x4); `docs/flutter_app_README.md:14` (wrong path `/Users/jhangyu/Documents/Halcyon/README.md`); `CLAUDE.md:26` ("It replaced `build.sh`... which are deleted"); `README.md:1` (`date: 2026-08-19`) | Delete `docs/flutter_app_README.md` (content fully superseded by root `README.md`, which already documents the Flutter-only single-codebase state), or if intentionally kept as a historical artifact, move it under `docs/logs/2026-05-01/` and strip the "跨檔同步對象" claim so it stops looking like a live doc. This is a content decision, not ours to execute — flag to user (R3-adjacent: deleting a root-adjacent doc). | high | high |
| `docs/logs/2026-08-23/round-1-m1-independent-review.txt` | One `.txt` file among ~100+ `.md` files under `docs/logs/`. Content and header format match the sibling `.md` review docs in the same folder (e.g. `round-1-ac11-independent-review.md`) — looks like a naming slip rather than an intentional format difference. | `docs/logs/2026-08-23/round-1-m1-independent-review.txt` (exists, confirmed); sibling `docs/logs/2026-08-23/round-1-ac11-independent-review.md` uses `.md` for the same document type | Rename to `.md` for consistency with rule.md's Task Log convention (which specifies `.md`). Low-risk, mechanical — but still a file rename, out of scope for this read-only audit to execute. | low | med |

## 2. Structure adjustments

| Path | Finding | Evidence (file:line) | Recommendation | Severity | Confidence |
|---|---|---|---|---|---|
| `docs/logs/` (root, all ~15 date folders) | 100+ files across 15 date-stamped folders with no index file anywhere (no `docs/logs/README.md`, no per-folder index). rule.md defines the folder convention (one log per task, dated) but never mandates or provides a retrieval index, and none of task.md/handover.md/memory.md aggregate a pointer table into the historical logs — task.md points only at the current ACTIVE log. Retrieval today is grep-by-filename or `ls` and read headers (as this audit had to do). | `rule.md:64-71` (docs/logs/ convention, no index requirement); `docs/logs/2026-08-23/`, `docs/logs/2026-08-24/`, `docs/logs/2026-08-25/` each hold 15-19 files (counted via `ls`) | Add a lightweight `docs/logs/INDEX.md` (or per-folder `docs/logs/YYYY-MM-DD/README.md`) listing filename + one-line purpose, updated at the same time a log is closed out (per rule.md's existing "Git 提交" step). This is additive-only, zero risk to existing files. Not urgent — logs are append-only history, not day-to-day navigation targets — but severity rises as folder count grows. | med | high |
| `docs/logs/2026-08-24/` and `2026-08-25/` (dumping-ground check) | `2026-08-24/` (19 files) mixes at least 4 distinct document families with no subgrouping: m3-m7 milestone handovers/contracts (11 files), a full "Task_refactor_plan_main.md" implementation plan (3280 lines) + its D1 variant (1707 lines) + a T9 handoff, a naming-audit trio, and a structure-audit trio that literal-strings-overlaps with the 2026-08-25 folder's own `structure-audit-*` files (see duplicates table, row 1). None of this is wrong per rule.md (rule.md only mandates date-folder + one-log-per-task), but the folder now functions as a dumping ground for at least 3 unrelated campaigns (image-pipeline M-series, tech-debt refactor plan, structure/naming audit) sharing one date. | `docs/logs/2026-08-24/` listing (19 entries, confirmed via `ls`); `docs/logs/2026-08-24/Task_refactor_plan_main.md:1` vs `docs/logs/2026-08-24/naming-audit-crosscutting.md` vs `docs/logs/2026-08-24/m6-execution-plan.md` — three unrelated campaign types, same folder | Not a rule.md violation (dating is by day, not by campaign) so no fix required under current SOP; if the team wants campaign-level grouping, that requires a rule.md amendment (subfolder-by-campaign-under-date), which is a process decision for the user, not this audit's call. Documenting as observation only. | low | high |
| Root-level 7 SOP `.md` files (`rule.md`, `task.md`, `memory.md`, `handover.md`, `plan.md`, `file_index.md`, `unit_test.md`) | Already assessed by the prior audit (`structure-audit-inventory.md:9`) as correctly placed at root per rule.md's own Startup Protocol (fast AI onboarding). Re-verified: rule.md's Startup Protocol block (`rule.md:13-21`) explicitly instructs reading these 6 files "before any work," which only works cheaply if they're at root, not nested under `docs/`. Concur with prior audit — no change recommended. | `rule.md:13-21` (Startup Protocol lists root-relative filenames, no `docs/` prefix) | Keep at root. Non-finding, listed here only because the assigned task explicitly asked the question. | n/a | high |
| `docs/superpowers/{plans,specs}/` | Already flagged by prior audit (`structure-audit-inventory.md:51`, `structure-audit-proposal.md:32`) as a third documentation convention not documented in rule.md. This audit independently verified it is NOT a duplicate of `docs/logs/` (see Duplicates §3, row on recycle-mode) — it is a distinct pre-implementation design-doc stage (spec → plan) that `docs/logs/` convergence-contracts explicitly cross-reference as authoritative source-of-truth documents, not something they duplicate. Concur with prior audit's "fold into rule.md as an official second convention" recommendation; no new finding to add beyond confirming it is legitimate durable content, not clutter. | `docs/logs/2026-08-18/recycle-mode-convergence-contract.md:9-11` ("Authoritative documents — Spec: docs/superpowers/specs/... Plan: docs/superpowers/plans/...") | No action beyond prior audit's recommendation (document the convention in rule.md). Deferred, per prior audit's parking-lot. | low | high |
| `docs/mockups/exif-rename/` (4 HTML variants) | Correctly named/scoped (rename-feature UI variants), correctly under `docs/`. No `docs/superpowers/specs/2026-08-19-exif-rename-design.md` cross-reference to these mockups was found (checked: spec doc does not mention `docs/mockups/`), so they're a slightly orphaned artifact relative to the exif-rename spec/plan pair, but this is a minor cross-linking gap, not a placement error. | `docs/mockups/exif-rename/` (4 files, confirmed via `ls`); `docs/superpowers/specs/2026-08-19-exif-rename-design.md` (no `mockups` string found) | No placement change needed. Optional: add a one-line pointer from the exif-rename spec to the mockups folder for discoverability. | low | med |
| `scripts/windows/README_WINDOWS.md` | Investigated per task prompt's question ("does this belong under docs/"). Verdict: correctly placed where it is — it is not a documentation-tree file, it is packaging payload. It ships *inside* the Windows hand-off zip (built by `scripts/package_windows.sh`) and its own text explicitly says "There is deliberately no copy of the build script at the zip root... only one of them would be the version under version control," establishing the coupling to `scripts/` is intentional. `file_index.md` already lists it correctly under `scripts/`. | `scripts/package_windows.sh:105` (`README_SRC="$HALCYON_DIR/scripts/windows/README_WINDOWS.md"`); `scripts/windows/README_WINDOWS.md:1-9`; `file_index.md:39` (`└── windows/README_WINDOWS.md  # 該 zip 的收件說明`) | No change. Non-finding. | n/a | high |

## 3. Duplicates

| Path | Finding | Evidence (file:line) | Recommendation | Severity | Confidence | Category |
|---|---|---|---|---|---|---|
| `docs/flutter_app_README.md` vs root `README.md` | Both describe the same Flutter app's features/build/install; `flutter_app_README.md` is a strict subset with stale build commands (see §1 row 1) and no unique content not already covered, more completely and more currently, by root `README.md`. | `docs/flutter_app_README.md:19-47` (feature list + `./scripts/build.sh` build section) vs `README.md:22-50` (superset feature table with the identical bullet items, e.g. "🔍 滑動視窗預載", "🗑 回收模式（.trash）/ macOS Trash", "🔄 Auto-advance") | **Redundant — recommend delete or archive**, not a historical record (it was never re-confirmed as current after `README.md` absorbed its content; SOP §"跨檔同步對象" in the file itself claims it should sync with `README.md`, i.e. it self-declares as a live duplicate, not an immutable log entry). | high | high | redundant |
| `docs/logs/2026-08-16/round-2-review.md` vs `round-2-review-2.md` vs `round-2-review-3.md` | Three sequential adversarial-review passes over three different commit HEADs (`5a8684e`→`bbde960`→`3cbc5ff`), each with a distinct verdict (2 blockers → 1 new blocker (fixed-verified) → 0 blockers/CLEARED) and distinct probe artifacts. Not the same content re-saved; each documents a different round's findings. | `round-2-review.md:1-6` (HEAD `5a8684e`, "REFUTED — 2 blockers"); `round-2-review-2.md:1-6` (HEAD `bbde960`, "1 new blocker (B3)"); `round-2-review-3.md:1-6` (HEAD `3cbc5ff`, "0 blockers. CLEARED.") | No action. | n/a | high | historical record — keep |
| `docs/logs/2026-08-16/round-2-plan.md` vs `round-2-implementation-plan.md` | Plan (contract/rationale, "收斂契約") vs its execution-oriented companion doc, explicitly cross-referenced as a pair, not a rewrite. | `round-2-plan.md:1-4` ("交接目的：讓執行 team 接續 round 2 實作"); `round-2-implementation-plan.md:1-4` ("交接目的：讓執行 team 的 fresh agent 依本檔直接開工，終態見 round-2-plan.md 驗收條件 1–8") — implementation-plan explicitly defers terminal-state authority to plan.md, i.e. two documents by design, not duplicate content | No action. | n/a | high | historical record — keep |
| `docs/logs/2026-08-16/round-3-plan.md` vs `round-3-implementation-plan.md` | Same pattern as round-2: implementation-plan explicitly states it revises 2 assumptions in plan.md based on new measurements, then defines phased execution — a derived, not duplicate, document. | `round-3-implementation-plan.md:1-4` ("本檔是 round-3-plan.md 的**執行版**：修正了原計畫兩處與實測不符的假設") | No action. | n/a | high | historical record — keep |
| `docs/logs/2026-08-25/Task_refactor_plan_main.md` vs `Task_refactor_plan_D1.md` vs `structure-refactor-plan.md` (note: the first two actually live in `docs/logs/2026-08-24/`, not `2026-08-25/` — see §1 non-finding note below) | Three different "refactor" plans, not duplicates: `Task_refactor_plan_main.md` = 9-task tech-debt/bugfix remediation plan (non-structural); `Task_refactor_plan_D1.md` = single-purpose `TierTwoRegistry` extraction plan (one of the 9 tasks' deep-dive); `structure-refactor-plan.md` (in 2026-08-25) = directory/file *placement* restructure plan, unrelated subject matter (moving files between `lib/services/` subfolders vs fixing bugs/extracting a class). Overlapping campaign name ("refactor") but disjoint scope and disjoint file lists. | `Task_refactor_plan_main.md:5` ("Fix 8 verified behaviour bugs... perform 3 structural extractions..."); `Task_refactor_plan_D1.md:5` ("Extract the tier-2 (full-size) ImageCache bookkeeping... into a new TierTwoRegistry class"); `structure-refactor-plan.md:1-8` ("Halcyon Structure Refactor — Implementation Plan... Source of truth: structure-audit-proposal.md") | No merge action. Naming-collision risk only (three unrelated docs all named `*refactor*plan*` across two adjacent date folders) — worth a one-line cross-reference note in each if a future reader might confuse them, but not a duplication defect. | low (naming-collision risk only) | high | historical record — keep |
| `docs/superpowers/plans/2026-08-17-recycle-mode.md` + `docs/superpowers/specs/2026-08-17-recycle-mode-design.md` vs `docs/logs/2026-08-18/recycle-mode-convergence-contract.md` | Not duplicates. Spec (design rationale) → Plan (task-by-task with code) → Contract (frozen terminal-state + acceptance criteria for the execution round) are three stages of the same feature, and the contract explicitly names the spec and plan as its "Authoritative documents," not superseding them. | `docs/superpowers/specs/2026-08-17-recycle-mode-design.md:1-4`; `docs/superpowers/plans/2026-08-17-recycle-mode.md:1-9`; `docs/logs/2026-08-18/recycle-mode-convergence-contract.md:9-11` | No action. | n/a | high | historical record — keep |
| `docs/logs/2026-08-23/*handover*.md` / `*handoff*.md` (11 files: `image-pipeline-redesign-handover.md`, `m0-complete-m1-next-handover.md`, `m3-midround-handover.md`, `m3-round-1-sonnet-takeover-handover.md`, `m4-m6-remaining-handover.md`, `round-2-lead-handover.md`, `m3-round-handoff.md`, `round-1-m0-handoff.md`, `round-1-m1-handoff.md`, `round-2-m2-handoff.md`) | Not duplicates. Each carries a distinct commit hash/worktree tip and covers a distinct milestone or round boundary (M0→M1, M1→M2, M2→M3 round1, M3 round1→round2, M3 round2 close, M3→M4-M6 remaining). Verified by reading the header block of every file: no two share the same "current tip" hash. | `m3-midround-handover.md:1-5` (tip `0e6407e`) vs `m3-round-1-sonnet-takeover-handover.md:1-5` (tip `867b6ee`) vs `round-2-lead-handover.md:1-4` (worktree `halcyon-m3`, M3 round 2) vs `m3-round-handoff.md:1-5` (lineage chain `3ffa4c4<-...<-1ba170a`) — four different M3-stage documents, four different hashes | No action — this is exactly the append-only Unified Task Log history rule.md prescribes for an evolving, multi-round milestone. | n/a | high | historical record — keep |

## Non-findings

- `file_index.md` path cross-check: spot-checked 9 representative paths from its directory tree against disk (`lib/services/image_pipeline/dng_embedded_jpeg_extractor.dart`, `test/services/image_pipeline/dng_embedded_jpeg_extractor_test.dart`, `lib/services/rename/rename_coordinator.dart`, `lib/models/rename_rule.dart`, `test/m6_bridge_free_test.dart`, `test/main_test.dart`, `test/widget_test.dart`, `test/support/synthetic_dng.dart`, `scripts/windows/README_WINDOWS.md`) — all exist. No stale paths found in the sample checked.
- Root `README.md` and `CLAUDE.md`: cross-checked build-command sections against actual `scripts/` contents — both correctly reference `scripts/build_apps.py` and correctly state `build.sh`/`build_windows.ps1`/`build_windows.py` are deleted (`CLAUDE.md:26`). No stale-file references found in these two.
- `docs/logs/YYYY-MM-DD/` filename-embedded-date vs folder-date check: scripted comparison across all ~15 date folders found zero mismatches (no file sitting under the wrong date directory).
- `docs/logs/2026-08-23/round-1-m1-independent-review.txt`: confirmed to be the one non-`.md` file among ~100+ log docs (see §1 for the finding; recorded here as the completed check for "is there a .txt file among the .md logs").
- `docs/mockups/exif-rename/`: 4 HTML files, content-appropriate for a mockups folder, no placement issue beyond the minor cross-link gap noted in §2.
- `docs/superpowers/plans/` and `docs/superpowers/specs/` content itself (all 8 + 4 files read via headers): confirmed durable design content, not scratch/debris; not duplicated by `docs/logs/`.
- Untracked 2026-08-24/2026-08-25 files named in the task prompt (`Task_refactor_T9_handoff.md`, `Task_refactor_plan_D1.md`, `Task_refactor_plan_main.md`, `p2-p4-remediation-contract.md`, `structure-audit-architecture.md`, `structure-audit-inventory.md`, `structure-audit-proposal.md`, `structure-refactor-baton-1.md`, `structure-refactor-plan.md`): all read, all legitimate non-duplicate content, all in git status as untracked-but-not-ignored (a commit/staging decision for the user, not a placement defect).
