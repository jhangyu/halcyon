# Structure Audit 2 — Infra (scripts/, assets/, web/, platform runners, root config)

Date: 2026-08-26
Scope: `scripts/**`, `assets/**`, `web/**`, `macos/**`, `ios/**`, `android/**`, `windows/**`, `linux/**`, root non-markdown config (`pubspec.yaml`, `pubspec.lock`, `analysis_options.yaml`, `dart_test.yaml`, `.metadata`, `.gitignore`, `.github/workflows/ci.yml`).
Method: read-only. All claims verified with `git ls-files`, `md5`, `diff`, `grep -rn`, and direct file reads. No file moved/edited/deleted.
Does NOT re-report items already decided in `docs/logs/2026-08-25/structure-audit-{inventory,architecture,proposal}.md` (gitignore fix for `.claude/`/`.codex`/`.pi/`, `AGENTS.md` dedupe, `tool/` vs `scripts/` consolidation as a generic idea, `scripts/tmp/` bulk cleanup after active rounds close) except where new evidence changes the prior verdict.

## 1. Misplaced files

| Path | Finding | Evidence | Recommendation | Severity | Confidence |
|---|---|---|---|---|---|
| `assets/icons/icon.png` | Byte-identical dead duplicate of `assets/icon.png`; unreferenced anywhere (pubspec `assets:` block is fully commented out; `flutter_launcher_icons.image_path` points only at `assets/icon.png`) | `MD5 (assets/icon.png) = e4cbc196ba82f6ba2c52b3e3452adb28` / `MD5 (assets/icons/icon.png) = e4cbc196ba82f6ba2c52b3e3452adb28`; `grep -rn "assets/icons" pubspec.yaml web/ lib/` → no hits | Delete `assets/icons/icon.png` (or the whole `assets/icons/` dir if `icon.svg` is also dropped, see next row) | med | high |
| `assets/icons/icon.svg` | Divergent duplicate (not identical) of `assets/icon.svg` — different SVG content, both unreferenced by any code/build config | `diff assets/icon.svg assets/icons/icon.svg` shows full content divergence (73 vs 79 lines); `grep -rn "assets/icon\.svg" pubspec.yaml web/ lib/ docs/` → no hits | Decide which SVG is the current source-of-truth design file and delete the other; neither is wired into any build step today, so this is source-art housekeeping, not a functional bug | low | high |
| `scripts/tmp/run_export_core_tests.sh` | References an `EXPORT-CORE-BEGIN/END` block in `macos/Runner/AppDelegate.swift` that no longer exists — export logic moved to pure Dart (`lib/services/library/photo_export_service.dart` per CLAUDE.md) | `grep -n "EXPORT-CORE" macos/Runner/AppDelegate.swift` → 0 matches; `grep -n "MethodChannel" macos/Runner/AppDelegate.swift` shows only `halcyon/trash` and `halcyon/open_with` | Delete; the shipped code it verifies no longer exists, so it cannot pass and is not scratch waiting to be reused, it's dead | high | high |
| `scripts/tmp/dng_extractor_tests.swift`, `probe_dng.swift`, `probe_extract.swift`, `probe_preview.sh`, `run_probe_extract.sh`, `run_dng_extractor_tests.sh`, `mutation_dng_extractor.sh`, `mutation_check.sh`, `mutation_why.sh` (9 of the 42 tracked `scripts/tmp/` files) | All target `macos/Runner/DngPreviewExtractor.swift`, which was deleted in the P3.1 macOS-native-removal task (2026-08-24) when DNG decode moved fully to the `dng_processor` native package / Dart pipeline. `docs/logs/2026-08-24/baseline-registry.md:67` explicitly records the Swift file + its Dart test as deleted with "受測實作已不存在" (tested implementation no longer exists) | `find . -iname "DngPreviewExtractor.swift"` → no matches; `find macos/Runner -name "*.swift"` → only `AppDelegate.swift`, `MainFlutterWindow.swift`; `docs/logs/2026-08-24/baseline-registry.md:67` | Delete all 9 files — their compile target is gone, they cannot run, and the prior structure-audit-inventory.md (2026-08-25 §2) flagged these as "possibly still-referenced (check before touching)" without doing the check; this audit did the check and the target is confirmed gone | high | high |
| `scripts/tmp/fixtures/*.dng` (6 synthetic DNG fixtures) | Fixtures generated by `scripts/tmp/make_synth_dng.py` specifically for the now-deleted Swift extractor test suite above; not referenced by any current `test/*.dart` | `grep -rn "synth_guards\|synth_multistrip\|synth_orient8\|synth_preexisting_exif\|synth_too_small\|synth_two_valid" test/ lib/` → 0 hits | Delete alongside the Swift test files they exist to feed | med | high |
| `scripts/tmp/raw_channel_order.py`, `rb_swap_test.py`, `perf_r2_recompute.py`, `perf_r3a_analysis.py`, `perf_r3a_switch.py` | Zero references anywhere in the repo (docs, scripts, build config) other than being named in the 2026-08-25 inventory audit's own catalogue | `grep -rn "<filename>" docs/ rule.md memory.md task.md handover.md plan.md file_index.md unit_test.md` → 0 hits for all 5 | Delete — no downstream consumer, no doc pointing back to them, last touched with the rest of `scripts/tmp/perf/` on 2026-08-16 | med | high |
| `scripts/tmp/perf/*` (18 files: wait_build*.sh ×5, parse*.py ×3, build.sh, run.sh, restore.sh, analyze_run.sh, contention_gap.py, make_dataset.py, split_dng_groups.py, split_dng_native.py, validate_run.py, validate_run_selftest.py, wp1_regression_check.py) | A closed round-3a perf-instrumentation harness. Last git-touched 2026-08-16 (`b557261`); zero mentions in current SOP docs (`plan.md`, `task.md`, `memory.md`) | `git log -1 --format=%ad -- scripts/tmp/perf/` → `Sun Aug 16 22:31:13 2026`; `grep -n "scripts/tmp/perf" plan.md task.md memory.md` → 0 hits | Not orphaned in the "broken" sense (internally self-consistent, files are genuinely distinct per-round variants, not accidental dupes — see §3), but it is closed-milestone scratch sitting in a directory whose own `.gitignore` comment says these 42 files are grandfathered, not blessed as permanent. Archive out of the tree (e.g. into `docs/logs/2026-08-16/` as a zip, or delete) rather than promoting to a first-class benchmark dir — nothing in current SOP treats round-3a perf work as ongoing | low | med |
| `assets/halcyon-windows-pack-20260822-154135.zip` (25MB) | A Windows hand-off package build output sitting inside `assets/` (a Flutter-asset-bundle-semantic directory), not under `artifacts/` (the directory this repo already designates for build outputs). Correctly gitignored via the blanket `*.zip` rule (`.gitignore:11`) so it carries no repo-bloat risk, but its location is accidental: `scripts/package_windows.sh:34` defaults `OUT_DIR="$(pwd)"` (cwd, not a fixed path), so this file only ends up in `assets/` if someone ran the script from inside `assets/` or passed `--out assets/` | `git check-ignore -v assets/halcyon-windows-pack-20260822-154135.zip` → matches `.gitignore:11: *.zip`; `grep -n "OUT_DIR" scripts/package_windows.sh` shows cwd-relative default | Move (outside git, a plain filesystem `mv`, not a git operation) to `artifacts/` or delete if stale; not a repo-tracked concern but confusing for anyone browsing `assets/` expecting only app icon sources | low | high |
| `windows/runner/halcyon_associations.reg` | Generated output (regenerated every Windows build by `scripts/gen_windows_associations.dart`, invoked from `scripts/build_apps.py:1478`) committed next to hand-written runner source (`halcyon_channels.cpp`, `halcyon_trash.cpp`, etc.) | `scripts/gen_windows_associations.dart:59-60`: `final out = File('windows/runner/halcyon_associations.reg'); out.writeAsStringSync(...)`; `scripts/build_apps.py:1475-1480` calls `dart run scripts/gen_windows_associations.dart` as part of every Windows build; not referenced from `windows/runner/CMakeLists.txt` or any `.cpp`/`.h` (`grep -rn "halcyon_associations.reg\|\.reg" windows/runner/CMakeLists.txt windows/runner/*.cpp windows/runner/*.h` → 0 hits), so it is not a build input, only a build output that happens to be checked in | Either gitignore it (it is regenerated deterministically from `SupportedPhotoFormats.supportedExtensions` on every Windows build per the `build_apps.py:1475` comment) or, if it's intentionally kept committed so a fresh non-Windows checkout still has a usable `.reg` file for manual double-click install without building, document that intent — currently there is no comment anywhere stating which of the two it is | low | med |
| `.github/workflows/ci.yml` | CI's `build` job runs `flutter build macos --release` directly, bypassing `scripts/build_apps.py` — which CLAUDE.md calls "the single build entry point" that per-platform scripts were deliberately deleted in favor of. The workflow also cannot currently run at all (own header comment: no git remote configured, and `flutter pub get` will fail on a fresh checkout due to the `../flutter_dng_decoder/dng_processor` path dependency) | `.github/workflows/ci.yml:1-7` (header comment), `:55-56` (`flutter build macos --release` step) | Not urgent to fix since the workflow has "never actually run" per its own comment, but flag for whoever eventually enables CI: the `build` job should call `python3 scripts/build_apps.py` instead of raw `flutter build`, otherwise it silently skips the S4 colour gate and native-dylib placement checks that CLAUDE.md says are mandatory | med | high |
| `scripts/gen_windows_associations.dart` | A Dart file under `scripts/`, which is wholesale-excluded from `flutter analyze` (`analysis_options.yaml:17-19`, with a documented reason: `scripts/tmp/r2_backup/` has broken imports and analyzing it turns the gate into noise). The exclusion is intentional and documented, but as a side effect this one *working*, still-invoked Dart file also never gets analyzed, unlike the structurally similar Dart tooling in `tool/m6_dng_gate/` (not excluded) | `analysis_options.yaml:17-19`; `find tool -type f` shows `tool/m6_dng_gate/*.dart` with no corresponding exclude entry | Low-priority: consider moving this one file to `tool/` (or narrowing the exclude to `scripts/tmp/**` instead of all of `scripts/**`) so it gets analyzer coverage, since it is real invoked build tooling, not throwaway scratch | low | med |

## 2. Structure adjustments

| Path | Finding | Evidence | Recommendation | Severity | Confidence |
|---|---|---|---|---|---|
| `scripts/` (flat) | Top level mixes Python (`build_apps.py`, `check_dng_ffi_artifacts.py`), JSON data (`dng_ffi_artifacts.json`), shell (`focus_forensics.sh`, `package_windows.sh`), a Dart file (`gen_windows_associations.dart`), a doc subtree (`windows/README_WINDOWS.md`), and a 42-file scratch subtree (`tmp/`) with no internal grouping by language or purpose | `git ls-files scripts \| grep -v '^scripts/tmp/'` → 7 top-level entries across 4 file types | Not urgent given the low count (7 non-tmp files); if `scripts/tmp/` gets cleaned per the 2026-08-25 audit's deferred item, the remaining flat set is small enough that no subfolder split is needed | low | med |
| `scripts/windows/README_WINDOWS.md` | Looks misplaced at first glance (doc under `scripts/`), but is in fact packaging-script *input*: `scripts/package_windows.sh:105,168` copies this exact file verbatim to the zip root it produces. Co-locating it with the script that consumes it is intentional, not accidental | `scripts/package_windows.sh:105`: `README_SRC="$HALCYON_DIR/scripts/windows/README_WINDOWS.md"`; `:168`: `cp "$README_SRC" "$STAGE_DIR/README_WINDOWS.md"` | No change — this is correctly placed once you know it's packaging input, not stray documentation. Worth a one-line comment at the top of the file itself saying "consumed by package_windows.sh, not a general doc" so a future placement audit doesn't re-flag it | low | high |
| `tool/` vs `scripts/` boundary | The two top-level tooling directories do have an implicit, mechanically-observable distinction not written down anywhere: `tool/` holds Dart files that ARE analyzed by `flutter analyze` (no exclude entry); `scripts/` holds the throwaway/analyzer-excluded tooling (Python/shell, plus the one Dart file discussed above). This corroborates but sharpens the 2026-08-25 audit's "consolidate tool/ into scripts/" suggestion — a straight merge would either start analyzing today's `scripts/tmp/r2_backup/` broken imports, or require carrying the exclude rule into a merged directory anyway | `analysis_options.yaml:17-19`; `find tool -type f` | If consolidating per the 2026-08-25 proposal, preserve the analyzed/non-analyzed split with a narrower exclude (e.g. `scripts/legacy/**` or per-file) rather than a blanket `scripts/**`, so gate-relevant Dart tooling doesn't silently lose analyzer coverage | low | med |
| `windows/runner/halcyon_*.cpp` vs `macos/Runner/AppDelegate.swift` | Not a duplication problem: Win32 (`halcyon_channels.cpp`, `halcyon_trash.cpp`) and Cocoa (`AppDelegate.swift`) implement the same two `MethodChannel` names (`halcyon/trash`, `halcyon/open_with`) in genuinely different platform APIs/languages. This is the expected shape for per-platform native bridges, not copy-paste that should be shared | `grep -n "halcyon/trash\|halcyon/open_with" windows/runner/halcyon_channels.cpp macos/Runner/AppDelegate.swift` both register the same two channel name strings via unrelated platform APIs | No action — flagging as a non-finding per the task brief's explicit ask to compare these files | n/a | high |
| `scripts/check_dng_ffi_artifacts.py` + `scripts/dng_ffi_artifacts.json` | Self-contained, well-documented (M7 Task 6) ABI/packaging checker with its own JSON manifest, but it is invoked from nowhere else in the repo — not from `scripts/build_apps.py`, not from `.github/workflows/ci.yml`, not from any doc describing it as a required gate step (unlike `tool/m6_dng_gate/`, which has a `run_gate.sh` and a `README.md` describing when to run it). The 2026-08-25 inventory audit called this "durable, still-referenced tooling, correctly placed. No findings" without checking whether anything actually calls it | `grep -n "check_dng_ffi_artifacts" scripts/build_apps.py` → 0 hits; `grep -rln "check_dng_ffi_artifacts.py" docs/logs/2026-08-2[0-9]/*.md` → only this audit's own predecessor mentions the filename | Correctly placed as a standalone manual tool; the only open question (user decision, not a placement defect) is whether it should be wired into `build_apps.py`'s Windows path or CI as an enforced gate, or intentionally stays manual-only | low | med |

## 3. Duplicates

| Path | Finding | Evidence | Recommendation | Severity | Confidence |
|---|---|---|---|---|---|
| `assets/icon.png` vs `assets/icons/icon.png` | Byte-identical | `MD5 (assets/icon.png) = e4cbc196ba82f6ba2c52b3e3452adb28` / `MD5 (assets/icons/icon.png) = e4cbc196ba82f6ba2c52b3e3452adb28` | See §1 — delete the `assets/icons/` copy | med | high |
| `assets/icon.svg` vs `assets/icons/icon.svg` | NOT identical — different content (confirmed divergent, not a duplicate) | `MD5 (assets/icon.svg) = 5ec2781617a745008a1cc07fb7527610` vs `MD5 (assets/icons/icon.svg) = 7087bbccb15cdba98ffe9a04841fcb34`; `diff` shows full structural divergence | See §1 — pick one as canonical, don't treat as accidental duplication | low | high |
| `web/icons/*.png` vs `assets/icons/*` | NOT duplicates — different filenames, different content, both are standard `flutter_launcher_icons`/PWA-generated outputs at their conventional locations | `md5` on all 4 `web/icons/Icon-*.png` files produced 4 distinct hashes, none matching `assets/icons/icon.png`'s hash | No action | n/a | high |
| `scripts/tmp/perf/wait_build.sh`, `wait_build2.sh`, `wait_build3.sh`, `wait_build4.sh`, `wait_build_debug.sh` | NOT duplicates — 5 distinct MD5s, genuinely different script iterations (not copy-paste residue) | `md5 scripts/tmp/perf/wait_build*.sh` → 5 distinct hashes (`c0e607...`, `c9b998...`, `7afb78...`, `9bc406...`, `b90b26...`) | No action — these are historically-distinct round iterations, not clutter from careless copying (though see §1 for the "closed milestone, should be archived" finding on the whole `perf/` dir) | n/a | high |
| `scripts/tmp/perf/parse.py`, `parse_burst.py`, `parse_r2.py`, `scripts/tmp/perf_r2_recompute.py`, `perf_r3a_analysis.py`, `perf_r3a_switch.py` | NOT duplicates — 6 distinct MD5s | `md5` on all 6 → 6 distinct hashes | No action | n/a | high |
| `scripts/tmp/perf/split_dng_groups.py` vs `split_dng_native.py` | NOT duplicates — different MD5s and substantively different docstrings/logic (one groups by nearest-neighbor matching for stats re-use, the other does FIFO-per-filename correlation into hit/miss/undetermined subsets) | `md5` → `dc2d008...` vs `15249b1...`; `diff` shows differing docstrings and algorithm description from line 2 | No action | n/a | high |
| `scripts/tmp/probe_dng.swift` vs `probe_extract.swift` | NOT duplicates — different MD5s, different purposes (`probe_dng.swift` is a raw CoreGraphics metadata dumper taking a CLI path arg; `probe_extract.swift` is an assertion-based `@main` test harness compiled together with the now-deleted `DngPreviewExtractor.swift`) | `md5` → `82af1a2...` vs `9b8423c...`; `diff` shows entirely different file bodies from line 1 | No action on duplication grounds; `probe_extract.swift` is however dead per §1 (compile target deleted) | n/a | high |

## Non-findings (checked and clean)

- `web/manifest.json`, `web/index.html` — reference only `web/icons/*` and `web/favicon.png`, no cross-references into `assets/`.
- `.gitignore` rules for `local_data/`, `DNG/`, `JPG/`, `coverage/`, `.idea/`, `*.iml`, `.vscode/`, `artifacts/{archives,build_cache,logs}/`, `/tmp/`, `__pycache__/`, `.claude/`, `.codex`, `.pi/` — none are no-ops; `git ls-files` shows zero tracked matches for any of these patterns, so each rule is doing real work (or correctly matches nothing tracked, which is not a contradiction since nothing was ever committed under them).
- `.gitignore:41` `scripts/tmp/` — confirmed accurate as currently worded (comment at `:36-38` correctly describes the 42 grandfathered tracked files; this is NOT the stale/contradictory comment the 2026-08-25 audit flagged — that has since been fixed).
- `windows/.gitignore` — standard Visual-Studio-artifact ignores (`x64/`, `x86/`, `*.suo`, `*.cache`), no overlap or contradiction with root `.gitignore`.
- `dart_test.yaml`, `.metadata`, `pubspec.lock` — standard Flutter-tool-managed files, correctly at root, no placement issues.
- `windows/runner/halcyon_*.cpp` vs `macos/Runner/AppDelegate.swift` — confirmed NOT duplicated logic worth sharing; genuinely platform-specific implementations of the same channel contract (see §2).
- `scripts/focus_forensics.sh` — confirmed still referenced (`docs/logs/2026-08-19/focus-stuck-after-display-wake.md`), correctly placed, not orphaned.
- `scripts/package_windows.sh` — confirmed still referenced (`README.md`, `docs/logs/2026-08-24/m6-execution-plan.md`, own header of `scripts/windows/README_WINDOWS.md`), correctly placed, not orphaned.
- `scripts/build_apps.py` invocation of `scripts/gen_windows_associations.dart` — confirmed wired correctly (`build_apps.py:1478`), output path matches `windows/runner/halcyon_associations.reg` exactly.

## Acceptance criteria status

1. This file exists with three tables titled exactly as required — PASS.
2. Every row has all 6 required columns — PASS.
3. Every byte-identity claim quotes actual `md5`/`diff` output — PASS.
4. Non-findings section present — PASS.
5. No repo file modified, moved, renamed, or deleted; no git state changed; no build run — PASS (read-only session, verified by `git status` showing no changes attributable to this audit beyond this new file).
