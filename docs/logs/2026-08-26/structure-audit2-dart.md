# Halcyon Dart Tree Follow-up Audit — lib/, test/, tool/

Date: 2026-08-26
Scope: `lib/**`, `test/**`, `tool/**` only (docs and infra trees owned by sibling audits).
Baseline: this audit runs AFTER the 2026-08-25 structure-audit → structure-refactor-plan campaign
executed (commits `7ebb56a`, `2bc8d8e`, `6bb3ea6`, `3a7f3bb`, `47bdca3`; current HEAD tree already
has `lib/services/{image_pipeline,library,rename,platform}/`, `lib/models/rename_rule.dart`,
`lib/views/rename_dialog/rename_dialog.dart`, and a mirrored `test/services/*` tree). All findings
below are NEW observations against the post-refactor tree, not re-reports of already-executed items.
Method: `Read`/`Grep`/`git diff -M` only — no code moved, edited, or deleted. codebase-memory MCP
tools were not present in this session's toolset; static grep/import-tracing was used instead
(same disclosed deviation as the 2026-08-25 architecture audit).

## 1. Misplaced files

| Path | Finding | Evidence (file:line) | Recommendation | Severity | Confidence |
|---|---|---|---|---|---|
| `lib/views/main_detail_view.dart:8` | View imports the perf-critical stateful controller directly, bypassing the documented `views → AppState → services` layering. This is a **known, previously-flagged, deliberately-deferred** issue (not new), carried forward here because it still exists on disk post-refactor. | `lib/views/main_detail_view.dart:8` — `import '../services/image_pipeline/image_preload_controller.dart';` (only `services/` import found across all of `lib/views/**`, confirmed via `grep -n "^import.*services/" lib/views/*.dart lib/views/rename_dialog/*.dart`) | No action this round — `docs/logs/2026-08-25/structure-refactor-plan.md` red line 5 explicitly parks this ("Finding 2's `main_detail_view` → `AppState` funnelling ... out of scope"). Re-flagging only so it isn't silently forgotten once the parking-lot is reviewed. | low | high |
| `test/widget_test.dart` | Flutter-template default filename; every other file in `test/` is named after its subject. Its `PhotoSelectorApp`-vs-`HalcyonApp` naming-audit finding has been fixed already (file now correctly imports/renders `HalcyonApp`), but the placement/naming issue (template-default filename) itself is still unresolved. | `test/widget_test.dart:1-33` (full file read; imports `package:halcyon_flutter/main.dart`, tests `HalcyonApp`) | Rename to `test/main_smoke_test.dart` or fold its one `testWidgets` case into `test/main_test.dart` (which already covers `main.dart`/`HalcyonApp` init paths — see §3). Low urgency, cosmetic. | low | high |

## 2. Structure adjustments

| Path | Finding | Evidence (file:line) | Recommendation | Severity | Confidence |
|---|---|---|---|---|---|
| `test/services/image_pipeline/*_m0_test.dart`, `*_f3_test.dart`, `*_m3_test.dart`, `*_m3_amend3_test.dart`, `*_m4_test.dart`, `*_m5_test.dart`, `test/m6_bridge_free_test.dart` | 7 test filenames still encode internal milestone/ticket jargon (M0/M3/M4/M5/M6/F3) rather than what they test. This is a carried-forward finding from the 2026-08-25 naming audit (informational only there); repeated here because it is still true post-refactor and is the most-visible remaining test-tree hygiene gap. | `test/services/image_pipeline/dng_embedded_jpeg_extractor_m0_test.dart`, `_f3_test.dart`; `test/services/image_pipeline/dng_nav_probe_m3_test.dart`; `test/services/image_pipeline/image_preload_controller_m3_amend3_test.dart`; `test/services/image_pipeline/image_preload_scheduling_m4_test.dart`; `test/services/image_pipeline/image_preload_dual_window_m5_test.dart`; `test/m6_bridge_free_test.dart` (all confirmed present via `find lib test tool -type f`) | No rename proposed by this audit either — content is genuinely distinct per-file (verified via `group()`/`test()` title grep, see §3), so this is a pure naming/discoverability nit, not a duplication risk. Leave for a dedicated naming pass if one is scheduled; do not fold into this round. | low | high |
| `test/views/rename_dialog_test.dart` sits flat at `test/views/`, not `test/views/rename_dialog/` | `lib/views/rename_dialog/` is a feature folder (5 files: `rename_dialog.dart` + 4 sub-widgets), but `test/` mirrors only the `lib/services/*` subfolder split (per the 2026-08-25 plan's Table E) — it does not mirror `lib/views/rename_dialog/`. This is consistent with the plan's own explicit scope (views subfolder mirroring was never in scope), not a defect, but it means `test/` mirrors `lib/`'s *service* layering exactly while leaving a `views/` asymmetry: one `lib/views/` subfolder exists with no `test/views/` counterpart. | `test/views/rename_dialog_test.dart:5` imports `package:halcyon_flutter/views/rename_dialog/rename_dialog.dart`; `find test/views -type f` shows no `rename_dialog/` subdir; cross-checked against `docs/logs/2026-08-25/structure-refactor-plan.md:172-188` (Table E) which only lists `test/views/` flat, confirming this was intentional scope, not an oversight | No action needed — intentional per the frozen plan. Noting only so a future contributor doesn't read the `views/` vs `services/` mirroring asymmetry as an accident. | low | high |
| `lib/views/rename_dialog/{actions,preview_list,rule_editor,section_label}.dart` have zero dedicated test files | Coverage gap, not placement — flagging only because it surfaced while checking test/lib mirroring; this is properly the testing-dimension reviewer's territory, not re-analyzed here. | `find lib/services lib/models lib/providers lib/views -name "*.dart" | ...` cross-referenced against `test/**/*_test.dart` presence (17 lib files total with no matching `_test.dart`, listed in full in Non-findings §, most are pre-existing/expected — e.g. pure data-shape files like `image_source_types.dart`, `dng_decode_contract.dart`) | Out of scope for this audit; hand to the testing-dimension reviewer if one runs. | n/a (not a placement finding) | high |

## 3. Duplicates

No file-level or test-body duplication was found. Detail per requested family:

- **`dng_embedded_jpeg_extractor_{endian,f3,m0,}_test.dart` (4 files)**: verified NOT duplicates — each covers a distinct concern confirmed via `group()`/`test()` titles read in full: `_endian_test.dart` → `'MM equals II'` / `'corruptOffsets'` (byte-order robustness); `_f3_test.dart` → single narrow regression (1 test, 65 lines); `_m0_test.dart` → `'AC3: longEdge 2800 is byte-identical...'` / `'AC6: no-candidate/missing/non-DNG inputs return null'`; `_test.dart` (main) → `'malformed/truncated/non-DNG input degrades to null'` / `'M7 ruling E: orientation is clamped to the EXIF-legal range 1..8'`. No shared test titles or bodies found across the four files.
- **`image_preload_controller*`/`image_preload_*` family (6 files)**: verified NOT duplicates — distinct `TC-NNN`/milestone-labelled test titles per file with zero title overlap (sampled first 3-6 titles per file via grep, see raw grep output referenced at `test/services/image_pipeline/{image_preload_controller_test,image_preload_controller_m3_amend3_test,image_preload_dual_window_m5_test,image_preload_scheduling_m4_test,image_preload_window_test,dng_nav_probe_m3_test}.dart`). This matches the controller's real size (`lib/services/image_pipeline/image_preload_controller.dart`, 898 lines) — six files splitting an 898-line surface's test coverage is proportionate, not redundant.
- **`photo_source*_test.dart` family (3 files)**: verified NOT duplicates — `photo_source_test.dart` covers general `PhotoSource` behaviour (5 tests); `photo_source_probe_test.dart` covers `probeSource` cost-gate cases TC-072..076 (imports only `photo_source.dart`); `photo_source_single_probe_test.dart` covers the "single file open" fused-probe seam, TC-090..094 (imports `dng_decode_contract.dart` + `dng_embedded_jpeg_extractor.dart` + `image_preload_controller.dart` in addition to `photo_source.dart`, i.e. an integration-level seam test, not a copy of the unit test). **One staleness finding inside this family**: `photo_source_probe_test.dart:23` still names its `group()` `'PhotoSource.probe (M3 cost gate)'`, but the `PhotoSource.probe()` static method it names was deleted by the 2026-08-25 P4b remediation (`docs/logs/2026-08-25/p2-p4-remediation-contract.md` AC-4) — the file's own test bodies correctly call the surviving `PhotoSource.probeSource(...)` (verified: `grep -n "PhotoSource\.\(probe\|probeSource\)" test/services/image_pipeline/photo_source_probe_test.dart` → 6 hits, all `probeSource`, 0 hits for bare `.probe(`), only the group *description string* is stale. **Severity: low, confidence: high** — cosmetic prose drift, not a functional or duplication issue; recommend a one-line group-title fix (`'PhotoSource.probeSource (M3 cost gate)'`) whenever that file is next touched.
- **`lib/services/image_pipeline/` scheduling machinery** (`cache_budget.dart` 39L, `photo_payload.dart` 67L, `photo_payload_cache.dart` 140L, `tier_two_registry.dart` 241L, `tier_two_scheduler.dart` 417L, `prefetch_scheduler.dart` 117L): read in full/near-full. **Not redundant** — each file's own doc-comment states a single, non-overlapping ownership boundary and the P4a extraction (`docs/logs/2026-08-25/p2-p4-remediation-contract.md` AC-3) already separated `TierTwoScheduler` out of `image_preload_controller.dart` for exactly this reason. `tier_two_registry.dart:1-24` explicitly documents the boundary with `tier_two_scheduler.dart` ("It owns state and nothing else. Scheduling ... stays in the controller/scheduler ..."); `prefetch_scheduler.dart:1-10` documents the `kExpensiveStartupRadius` vs `kTierTwoRadius` split as deliberately non-mergeable (regression risk cited: TC-098). No overlapping responsibility found.

## Non-findings

Checked and found clean (no placement issue):

- **`lib/services/` 4-subfolder split** (`image_pipeline/` 18 files, `library/` 4 files, `rename/` 3 files, `platform/` 2 files) — every file sits in the subfolder matching its cluster per the already-executed 2026-08-25 plan; spot-verified `lib/services/library/*`, `lib/services/rename/*`, `lib/services/platform/*` directory listings against the plan's Table A.
- **Frozen integration seam** — `dng_decode_contract.dart` and `image_source_types.dart` are byte-identical to the pre-refactor baseline: `git diff refactor/structure-base..HEAD -M -- '*dng_decode_contract.dart' '*image_source_types.dart'` shows two 100%-similarity renames, zero content lines.
- **`rename_rule.dart` reclassified as a model** — all 12 importers across `lib/` and `test/` (grepped in full) correctly reference `models/rename_rule.dart`; zero stale `services/rename_rule.dart` references remain.
- **`tool/m6_dng_gate/`** — genuinely a tool (not misplaced): self-contained benchmark harness with its own README documenting provenance/pre-registration discipline; both `.dart` benchmark files' imports already point at post-refactor `services/image_pipeline/*` paths (verified, zero stale imports).
- **`lib/perf/` vs `test/perf/`** — `lib/perf/perf_driver.dart` and `perf_log.dart` are documented, deliberate "permanent instrumentation, structural no-op unless `HALCYON_PERF_DIR` is set" (headers read in full); correctly placed as production instrumentation, not test code. `test/perf/perf_log_build_stamp_test.dart` correctly tests only `perf_log.dart`'s compile-time constant (perf_driver.dart is UI-driven and per user directive is not agent-testable — a testing-dimension note, not a placement issue).
- **`test/support/synthetic_dng.dart`** — correctly isolated as a shared fixture, not a test file itself; all 3 importers use the correct post-refactor relative path (`../../support/synthetic_dng.dart`, 2 levels up from `test/services/image_pipeline/`), matching the deviation fix recorded in `docs/logs/2026-08-25/structure-refactor-baton-1.md` deviation #3.
- **Dead files** — no `lib/` file has zero references from the rest of the tree. Checked every `lib/**/*.dart` (`grep -rl <basename> lib test tool`, excluding self); the only sub-3-reference files (`trash_service.dart`, `batch_delete_feedback.dart`, `preview_list.dart`, `rule_editor.dart`, `settings_dialog.dart`) each have exactly 1 real importer, consistent with being leaf feature files, not orphans.
- **`file_index.md` path references** (in-scope subset only — full doc audit belongs to the docs-tree teammate) — extracted 31 backtick-quoted `lib/`/`test/`/`tool/` paths via regex; all 31 resolve to real files on disk. Zero dangling references found in the subset this audit checked.
- **`readDngOrientation` dead code** — confirmed still present and still production-dead (`lib/services/image_pipeline/dng_embedded_jpeg_extractor.dart:272`, zero callers outside that file and its own tests) — this is a **carried-forward** finding from the 2026-08-25 naming audit (already disclosed there as "low severity, no rename proposed"), not a new discovery; not re-tabled above to avoid double-counting, but confirmed still true post-refactor.
- **`PhotoSource.probe()` removal (P4b)** — confirmed fully executed: `grep -n "static Future<SourceCost?> probe" lib/` → 0 hits; all call sites (`dng_nav_probe_m3_test.dart:152`, `photo_source_probe_test.dart` ×6) already rewritten to `probeSource`.
- **`test/main_test.dart` vs `test/widget_test.dart` overlap** — read both in full; zero scenario overlap (`main_test.dart` covers `configureImageCache` sizing, file-drop DropTarget, keyboard shortcuts, dialog-stacking; `widget_test.dart` covers only the empty-folder-prompt smoke render). Not a duplicate pair, despite the naming/placement nit noted in §1.
