# macOS
.DS_Store

# Build and package artifacts
.build/
build/
**/build/
artifacts/archives/
artifacts/build_cache/
artifacts/logs/
*.zip

# Local photo datasets
local_data/
DNG/
JPG/

# Flutter / Dart
.dart_tool/
**/.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
**/doc/api/
**/ios/Flutter/.last_build_id
.pub-cache/
.pub/
coverage/
**/coverage/

# IDE
.idea/
*.iml
.vscode/

# Scratch verification output — never committed (CLAUDE.md scratch-lane rule).
# NOTE: 42 files under scripts/tmp/ were tracked before this rule was added
# (perf harness, DNG fixtures, mutation/gate runner scripts); gitignore does not
# untrack existing files, so those stay tracked until a separate decision is made.
/tmp/
__pycache__/
scripts/tmp/
# Agent-tooling state (not project source)
.claude/
.codex
.pi/
