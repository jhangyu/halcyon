# macOS
.DS_Store

# Build and package artifacts
.build/
build/
**/build/
artifacts/archives/
artifacts/build_cache/
artifacts/logs/
*.zip

# Local photo datasets
local_data/
DNG/
JPG/

# Flutter / Dart
.dart_tool/
**/.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
**/doc/api/
**/ios/Flutter/.last_build_id
.pub-cache/
.pub/
coverage/
**/coverage/

# IDE
.idea/
*.iml
.vscode/

# Scratch verification output — never committed (CLAUDE.md scratch-lane rule).
# Files formerly tracked under scripts/tmp/ were removed from the index during
# the 2026-08-26 structure cleanup; scripts/tmp/ is now fully untracked.
/tmp/
__pycache__/
scripts/tmp/
# Agent-tooling state (not project source)
.claude/
.codex
.pi/

# Regenerated by scripts/gen_windows_associations.dart on every Windows build.
windows/runner/halcyon_associations.reg

# Internal project maintenance documents (SOP set + CLAUDE.md), deliberately kept
# out of version control by a 2026-08-26 decision. See docs/logs/2026-08-26/
# sop-relocation-contract.md for the rationale.
docs/sop/
CLAUDE.md
