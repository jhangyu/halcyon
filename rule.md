# 開發標準作業程序 (Development S.O.P.)

為了避免專案上下文長度過長導致 AI 遺失記憶 (Context Loss)，並建立完整可追溯的開發歷史，此專案嚴格遵守以下由時間戳記驅動的文件化開發流程。

---

## 🚀 新對話啟動指令 (Startup Protocol)

> **每次開啟新對話，使用者請直接複製以下文字貼到最開頭，強制 AI 執行環境掃描：**

```
請先依序閱讀以下文件並進行環境掃描，再開始任何工作：
1. rule.md — 開發標準作業程序
2. memory.md — 每輪對話結束前更新，用來銜接ai上下文斷點的進度快照與避坑指南
3. task.md — 任務真實狀態來源，**特別注意頂部「🔴 現在進行中 (ACTIVE)」區塊**
4. handover.md — 短期交接摘要（中斷點、已完成、下一步）
5. plan.md — 專案中長期目標、階段里程碑與交付規劃
6. file_index.md — 專案檔案地圖與目錄對照，優先用來定位檔案
7. unit_test.md — 測試策略、成功標準、測試/編譯腳本登錄資訊
8. 根據 task.md 取得當前執行的 Unified Task Log 紀錄檔（docs/logs/YYYY-MM-DD/...）

若上述任一核心檔案不存在（memory.md / task.md / handover.md / plan.md / file_index.md / unit_test.md）：
9. 先掃描專案目錄與現有程式碼（至少含 src/、tests/、docs/、設定檔），再依「核心文件職責劃分與維護政策」章節補建缺失檔案。
10. 補建時禁止建立空白模板；每個新檔案開頭必須先定義「檔案維護政策」區塊（含：用途、更新時機、必填欄位、跨檔同步對象）。
11. 補建後必須回報：哪些檔案為新建、內容依據來源、仍待使用者確認的假設。

完成掃描後，先輸出一份簡短的「現狀確認」，說明你對當前斷點的理解，再等待我的指示。
```

### Startup Protocol 補充規範（缺檔自動補建）

1. 缺檔判定：
   - 以專案根目錄實際檔案為準，不憑記憶假設檔案存在。
   - 若檔名大小寫或副檔名不一致（例如 `RULE.MD` vs `rule.md`），先在現狀確認中標示風險，再採用專案既有慣例。
2. 內容生成依據：
   - 優先來源：現有程式碼、測試、設定檔、既有 docs/logs。
   - 次要來源：其他核心文件中已存在且可互相驗證的資訊。
   - 若資訊不足，必須明確標註「假設」與「待確認事項」，不得捏造版本、測試數量或完成率。
3. 新建檔案最低結構要求：
   - YAML frontmatter（至少 `date`、`title`）。
   - `## 🧭 檔案維護政策`（用途、更新時機、必填欄位、跨檔同步規則）。
   - 與該檔案職責對應的核心內容（不可僅有標題）。
4. 同步一致性要求：
   - 若補建 `file_index.md`，必須同步反映目前真實目錄結構。
   - 若補建 `task.md`，必須建立 `🔴 現在進行中 (ACTIVE)` 區塊。
   - 若補建 `memory.md`，必須建立架構決策與 gotchas 區塊。
   - 若補建 `unit_test.md`，必須建立測試策略、成功標準與執行指令。
5. 安全護欄：
   - 不得覆蓋使用者既有內容；若檔案存在但內容疑似損毀，先備份或在原檔追加，不直接重寫整份。
   - 不得把暫存結果（如 screenshots、test-results）誤當正式規格。
6. 完成回報格式：
   - 列出「新增/更新檔案清單」。
   - 列出「跨檔同步清單」。
   - 列出「假設與待確認事項」。

---

## 目錄結構與檔案索引約定
為了避免模型在尋找測試或原始程式碼時浪費 Token，請查閱 [**`file_index.md`**](file_index.md)，內含全專案各資料夾與重要檔案的定義。

所有開發過程產生的紀錄檔均需放置於專屬的 `docs/logs/` 目錄內，並按照日期進行分類，以維持專案根目錄的整潔。

```text
docs/
└── logs/
    └── YYYY-MM-DD/
        └── Task_6.4_Zero_Copy.md    # 單一任務日誌統一個檔 (Unified Task Log)
```

---

## 📝 核心文件職責劃分與維護政策 (Single Source of Truth)

1. **`task.md` (任務真實狀態來源)**
   - 負責管理所有任務清單與執行狀態，是「現在要做什麼」與「做到哪裡」的唯一真實來源。
   - **🔴 現在進行中 (ACTIVE)** 同時間只保留一個主任務；切換任務時必須同步更新狀態、摘要與下一步。
   - 任務敘述需可驗證（例如測試數量、功能完成標準），避免模糊描述。
   - 已完成事項若超過 2 個迭代，應摘要沉澱到 `memory.md` 或 `plan.md`，保持可讀性。

2. **`memory.md` (全域知識庫與避坑指南)**
   - **每輪對話結束前必須更新**，用於銜接上下文斷點。
   - 記錄架構決策、踩坑紀錄 (Gotchas)、重要注意事項。
   - 一旦發現新地雷或做出架構決策，必須即時更新，避免重複踩坑。
   - 本檔聚焦長期知識，不重複維護短期執行細節。

3. **`plan.md` (中長期里程碑與路線圖)**
   - 紀錄高階目標與各階段 (Phase) 進度，不作為即時進度看板。
   - 每個階段需有明確狀態（已完成/進行中/待辦）與可驗證產出。
   - 階段完成後保留關鍵決策與驗證結果，移除過時草稿，避免重複與衝突內容。

4. **`file_index.md` (檔案地圖與入口索引)**
   - 全專案唯一檔案與目錄對照表，模型應優先依此定位檔案，避免無謂全域搜索。
   - 新增、搬移、刪除核心檔案/資料夾時，必須同次提交更新目錄樹與說明。
   - 目錄樹維持專案絕對路徑開頭，說明精簡且可快速 onboarding。

5. **`unit_test.md` (測試策略與品質門檻)**
   - 維護測試策略、測試矩陣、成功標準；不放功能實作細節。
   - 新增功能時，必須同步補上對應測試案例（ID、名稱、預期結果）與成功標準。
   - 測試 ID 不可重複；移除或改名需註記原因並保持矩陣可追溯。
   - 測試指令需可直接執行；若命令或路徑調整，必須同次更新。

6. **`handover.md` (短期交接摘要)**
   - 記錄當前衝刺最重要的中斷點、已完成項目、下一步與決策摘要。
   - 每次更新需同步檢查 `當前任務`、`已完成項目`、`下一步` 三區塊一致性。
   - `下一步` 只保留 3-5 個可立即執行項；完成後移至 `已完成項目` 或 `task.md`。
   - Bug 結案後，本檔僅保留簡要結論，長期知識移入 `memory.md`。

### 跨檔案同步規則 (Cross-file Consistency)

1. 更新 `task.md` 的 ACTIVE 區塊時，必須同步檢查 `handover.md` 的 `下一步` 是否一致。
2. 新增/刪除核心檔案時，必須同步更新 `file_index.md` 與 `plan.md` 的交付物清單。
3. 新增架構決策或新 gotcha 時，必須同步更新 `memory.md`；`plan.md` 只保留里程碑層級摘要。
4. 測試總數、測試策略或門檻變更時，必須同步更新 `unit_test.md`，並回寫 `task.md`/`memory.md` 的測試統計。
5. 各核心文件的 `date` 欄位需反映最後一次有效更新日期（`YYYY-MM-DD`）。

---

## 標準開發流程 (Unified Task Log 推進法)

對於每一項特定的任務 (Task 或 Phase 子項目)，請在 `docs/logs/YYYY-MM-DD/` 下建立**唯一**的一份 Markdown 日誌 (例如 `Task_6.4_Zero_Copy.md`)。
所有的開發循環與記錄都疊加在此單一檔案中進行。這樣可消除檔案碎片化並提升連貫性：

### 階段 1. [環境掃描] Summary (現狀總結)
- **時機**：開啟新任務時。
- **動作**：在該階段 Task 日誌的最開始寫入 `## 1. Summary`，並總結目標與現況。

### 階段 2. [計畫制定] Implementation Plan (實作計畫)
- **時機**：確認目標後，在實際修改任何程式碼前。
- **動作**：在該日誌中疊加 `## 2. Implementation Plan`，並規畫具體修改的檔案與邏輯。

### 階段 3. [開發執行與記錄] Execution (執行日誌)
- **時機**：開始修改代碼與測試的循環。
- **動作**：寫入 `## 3. Execution Log`。
  - 必須維護一個隨著操作隨時覆寫的快照區塊：
    ```markdown
    ### ⏹️ 中斷點快照 (Breakpoint Snapshot)
    - **已完成**: `具體完成的子步驟`
    - **下一步**: `接下來要做的子步驟`
    - **待確認**: `需要使用者決策的事項`
    ```
  - 取代過去分散的 debug 與 memory 操作，將修改結果、測試錯誤、修正與除錯紀錄持續往下方追加。

### 階段 4. [成果驗證] Walkthrough (驗證報告)
- **時機**：任務順利完成並通過測試。
- **動作**：寫入 `## 4. Walkthrough`，總結成果數據與測試證明（如截圖、視訊、PSNR）。

> **⚠️ Token 壓力警示規則**：當 AI 預估目前的對話內容已達到約 60-70% 上下文容量時，必須主動提示使用者建議中斷對話，並在發出警示前，**強制將當下的執行進度更新於 `task.md` 頂部的 (ACTIVE) 區塊** 以保存狀態。

---

## [重要] 狀態同步與 Git 提交 (Git & CI/CD Workflow)
- **時機**：單一任務階段 (Task) 或是重大 Bug 修補成功完成後。
- **動作**：
  1. 將任務狀態打勾，更新 `task.md` 及 `plan.md` 進度。
  2. 若任務中發現重要 Bug 的解法或框架方針決策，立即同步沉澱技術知識至 `memory.md`。
  3. 確保所有 Log 保存完畢，執行 `git add` 準備提交。
  4. **嚴格遵守 Conventional Commits 規範** (feat/fix/docs/refactor/chore 等)，例如 `git commit -m "feat(halide): unify gpu tile structure"`。
- **決策中繼點**：成功則自動進行下一個 Task；若失敗卡關則在日誌中總結死胡同，向使用者回報要求評估下一步策略。

---

## `task.md` 頂部指針區塊格式 (ACTIVE)
`task.md` 頂部必須無時無刻維護此表格，做為整個 AI 的進度看板：

```markdown
## 🔴 現在進行中 (ACTIVE)
- **Task**: 6.4 Zero-copy 機制
- **Log File**: [Task_6.4_Zero_Copy.md](docs/logs/YYYY-MM-DD/Task_6.4_Zero_Copy.md)
- **中斷點與交接 (Handover)**: 已完成 C++ 端的 `free()` 綁定，下一步要修改 `dng_decoder_service.dart` 納入 NativeFinalizer。
```

---

## Task 日誌檔首的 YAML Frontmatter
所有位於 `docs/logs/` 的 Unified Task 日誌，檔案開頭都**必須**包含：

```markdown
---
date: YYYY-MM-DDTHH:MM:SS
task: "任務名稱 (對應 task.md)"
status: in_progress | success | failed
---
```

---

## Appendix A: 程式碼檔案的 YAML 索引規範 (Long File Index)
- **適用對象**：大於約 100 行或是包含多個邏輯模組的單一程式碼檔案 (例：`HalidePipeline.cpp`)。
- **原則**：在此類檔案的最開頭 (第一行起始的區塊註解內)，建立一份 YAML 格式索引。
- **讀取限制**：模型在理解或準備修改該檔案前，**強制規定只能先用 `view_file` (StartLine=1, EndLine=100) 讀取前 100 行來獲取 Index**。獲取後再以精準行數抓取所需方法，**絕對禁止自行一次性載入整個大檔案**。
- **範例格式**：
  ```cpp
  /*
  ---
  file_summary: "簡述檔案整體用途"
  functions:
    - name: "HalidePipeline::process"
      description: "管線主入口邏輯"
      lines: "1000-1150"
  ---
  */
  ```
- **維護責任**：新增、刪除 Function、或是跨越多行修改導致行號失準時，**模型有責任回查該檔案頂部的索引，並至少更新一次 `lines` 或 `description`**，確保索引隨程式碼同步保鮮。

---

## Appendix B: 測試與編譯腳本強制規範

### B.1 目標與適用範圍
- 凡屬「可重複執行」行為，包含測試、編譯、打包、回歸驗證、輸出比對，一律腳本化處理。
- 禁止以一次性手動命令取代可腳本化流程（除非緊急除錯且已記錄原因）。
- 所有腳本行為必須可被他人重跑並得到可比較結果（可重現性）。

### B.2 既有腳本優先原則
- 執行前先搜尋專案既有腳本（例如 `tools/`、`scripts/`、`tests/`、`package.json` scripts）。
- 若既有腳本可覆蓋需求，必須直接使用，不得另寫平行版本。
- 若既有腳本不足，優先「擴充原腳本」而非新建一次性腳本。
- 僅在無法擴充時才可新增腳本，並在 Task Log 記錄新增原因與替代方案評估。

### B.3 契約規範（Contract-First）
- 所有測試或編譯腳本在進入主流程前，必須先做契約檢查並輸出明確結果（`[Contract] PASS` 或 `[Contract] FAIL`）。
- 最低契約檢查項目：
  1. 輸入存在性（檔案、路徑、必要環境變數）
  2. 版本相容性（runtime / compiler / dependency）
  3. 介面契約（必要參數、輸出格式、退出碼語義）
  4. 資料契約（必要欄位、型別、尺寸/shape、非空約束）
  5. 輸出契約（輸出目錄、檔名規則、log 可追溯欄位）
- 契約失敗必須立即中止，不得繼續執行後續測試或編譯。

### B.4 腳本設計與輸出標準
- 腳本需支援非互動執行（CI 友善），避免依賴人工輸入。
- 腳本需回傳明確 exit code：`0` 成功，非 `0` 失敗。
- 輸出至少包含：
  - 本次目標（test/build/package）
  - 執行命令與主要參數
  - 契約檢查結果
  - 成功/失敗摘要與失敗原因
- 同一用途腳本的輸出格式應一致，避免不同人難以解析。

### B.5 命名與存放規範
- 測試與編譯腳本集中於固定目錄（建議 `tools/` 或 `scripts/`），避免散落。
- 命名採動詞導向與可讀格式，例如：
  - `run_unit_tests.*`
  - `build_prod.*`
  - `run_regression_matrix.*`
- 禁止使用 `temp_*`、`final_*`、`new_*` 這類不可維護命名。

### B.6 重複性作業強制腳本化條件
- 任一流程符合下列任一條件，即必須腳本化：
  1. 預期會再執行第 2 次
  2. 步驟超過 2 條命令
  3. 牽涉多個環境前置檢查
  4. 結果需進行跨版本比較

### B.7 `unit_test.md` 同步登錄規範（強制）
- 任何新增、修改、淘汰的測試/編譯腳本，必須在同次提交同步更新 `unit_test.md`。
- `unit_test.md` 至少記錄：
  1. 腳本名稱與路徑
  2. 用途與適用場景
  3. 必要輸入參數與範例指令
  4. 契約檢查項目摘要
  5. 主要輸出與成功判定標準
  6. 常見失敗與排查方向
- 若 `unit_test.md` 未同步，該腳本視為「未完成交付」。

### B.8 變更治理與淘汰機制
- 腳本行為變更（參數、輸出、判定邏輯）必須在變更說明中標註相容性影響。
- 新腳本上線後若與舊腳本重疊，需在 1-2 次迭代內完成整併，避免雙軌。
- 廢棄腳本需標記 deprecate 狀態與替代腳本，並更新 `unit_test.md`。

### B.9 禁止事項
- 禁止只在對話文字中提供命令而不落地為腳本（針對重複性流程）。
- 禁止跳過契約檢查直接執行測試或編譯。
- 禁止新增不可重現的一次性「本機特化」腳本（硬編碼個人路徑、手動 UI 步驟）。
